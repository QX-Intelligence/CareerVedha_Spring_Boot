name: Build and Deploy Spring Service

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_TAG: ${{ github.sha }}
      ECR_REGISTRY: 264426277863.dkr.ecr.ap-south-2.amazonaws.com
      ECR_REPOSITORY: springservice
      AWS_REGION: ap-south-2

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push ARM Image (NO CACHE)
        run: |
          docker buildx build \
            --no-cache \
            --pull \
            --platform linux/arm64 \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:latest \
            --push .

      - name: Deploy to EC2 (Clean & Recreate)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            aws ecr get-login-password --region ap-south-2 \
            | docker login --username AWS --password-stdin $ECR_REGISTRY

            cd /home/ec2-user/spring/

            # Stop and remove old container
            docker-compose down --remove-orphans || true
            docker rm -f spring-api || true

            # Remove old image to prevent stale reuse
            docker rmi $ECR_REGISTRY/$ECR_REPOSITORY:latest || true

            # Pull fresh image
            docker pull $ECR_REGISTRY/$ECR_REPOSITORY:latest

            # Start fresh container
            docker-compose up -d --force-recreate

            # Show running container image ID
            docker ps
            docker images | grep springservice
